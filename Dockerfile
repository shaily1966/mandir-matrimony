FROM python:3.12-slim`nWORKDIR /app`nRUN apt-get update && apt-get install -y gcc g++ libpq-dev && rm -rf /var/lib/apt/lists/*`nCOPY requirements.txt .`nRUN pip install --no-cache-dir -r requirements.txt`nCOPY . .`nEXPOSE 8000`nCMD ["uvicorn", "backend.main:app", "--host", "0.0.0.0", "--port", "8000"]
